; CMHG template, generated by CMunge 0.77 (13 Jun 2006)
;
; Customise this to fit the module you are writing

#include "VersionNum"

; If you declare module-is-runnable the module will be entered at the main()
; function in user mode (ie like a normal C program).
; You will probably never need this field.
;module-is-runnable: 

; When your module cannot be reentered you should declare this. At present
; it does nothing.
; You will probably never need this field.
;module-is-not-reentrant: 

; When your module includes C++ code built using the CFront compiler, you
; should declare this. As part of the module initialisation sequence, the
; C++ global constructors will be called. Essentially this removes the
; requirement to manually invoke ______main.
;module-is-c-plus-plus: 

; You should give your module a sensible name which describes its purpose
; both accurately and concisely. You must register any module names you use
; if they are to be distributed.
title-string: FilterManager

help-string: FilterManager Module_MajorVersion_CMHG

swi-chunk-base-number: &42640

swi-handler-code: Mod_SWI

swi-decoding-table: Filter,
                   RegisterPreFilter,
                   RegisterPostFilter,
                   DeRegisterPreFilter,
                   DeRegisterPostFilter,
                   RegisterRectFilter,
                   DeRegisterRectFilter,
                   RegisterCopyFilter,
                   DeRegisterCopyFilter,
                   RegisterPostRectFilter,
                   DeRegisterPostRectFilter,
                   RegisterPostIconFilter,
                   DeRegisterPostIconFilter,
                   RegisterIconBorderFilter,
                   DeRegisterIconBorderFilter

error-base: &81D400 ; UNALLOCATED

error-identifiers: \
    Err_FilterAlreadyRegistered("Filter already registered"), \
    Err_FilterNotRegistered("Filter not registered"), \
    Err_NoMoreRoomForFilters("No more room for filters"), \
    Err_InvalidParameters("Invalid parameters")

initialisation-code: Mod_Init

finalisation-code: Mod_Final

generic-veneers: StartupCallback_Entry/StartupCallback_Handler,
                 Filter_PrePoll_Entry/Filter_PrePoll_Handler,
                 Filter_PostPoll_Entry/Filter_PostPoll_Handler,
                 Filter_BlockCopy_Entry/Filter_BlockCopy_Handler,
                 Filter_GetRectangle_Entry/Filter_GetRectangle_Handler,
                 Filter_PostGetRectangle_Entry/Filter_PostGetRectangle_Handler,
                 Filter_PostIconGetRectangle_Entry/Filter_PostIconGetRectangle_Handler,
                 Filter_IconBorder_Entry/Filter_IconBorder_Handler


command-keyword-table: Mod_Command
   Filters(min-args: 0, max-args: 0, help-text: "*Filters lists the registered filters.", invalid-syntax: "Syntax: *Filters")

service-call-handler: Mod_Service &53 &87 &88


